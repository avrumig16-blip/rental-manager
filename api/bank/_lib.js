const crypto=require("crypto");
const API="https://api.enablebanking.com";
function b64url(v){return Buffer.from(v).toString("base64url")}
function jwt(){const app=process.env.ENABLE_BANKING_APP_ID,key=(process.env.ENABLE_BANKING_PRIVATE_KEY||"").replace(/\\n/g,"\n");if(!app||!key)throw new Error("Missing ENABLE_BANKING_APP_ID or ENABLE_BANKING_PRIVATE_KEY");const now=Math.floor(Date.now()/1000),header=b64url(JSON.stringify({typ:"JWT",alg:"RS256",kid:app})),body=b64url(JSON.stringify({iss:"enablebanking.com",aud:"api.enablebanking.com",iat:now,exp:now+3600})),input=`${header}.${body}`,sig=crypto.sign("RSA-SHA256",Buffer.from(input),key).toString("base64url");return`${input}.${sig}`}
async function eb(path,options={}){const r=await fetch(API+path,{...options,headers:{Authorization:`Bearer ${jwt()}`,Accept:"application/json",...(options.body?{"Content-Type":"application/json"}:{}),...(options.headers||{})}});const txt=await r.text();let data;try{data=JSON.parse(txt)}catch{data={message:txt}}if(!r.ok)throw new Error(data.detail||data.message||data.error||`Enable Banking HTTP ${r.status}`);return data}
async function supa(path,options={}){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY");const r=await fetch(`${url}/rest/v1/${path}`,{...options,headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:options.prefer||"return=representation",...(options.headers||{})}});const txt=await r.text();let data=txt?JSON.parse(txt):null;if(!r.ok)throw new Error(data?.message||`Supabase HTTP ${r.status}`);return data}
module.exports={eb,supa};
