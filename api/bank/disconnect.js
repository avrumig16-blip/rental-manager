const {eb,supa}=require('./_lib');
module.exports=async(req,res)=>{try{if(req.method!=='POST')return res.status(405).json({error:'POST required'});const id=String(req.query.connection_id||'');if(!/^\d+$/.test(id))return res.status(400).json({error:'Invalid connection'});const rows=await supa(`bank_connections?id=eq.${id}&select=*`),c=rows&&rows[0];if(!c)return res.status(404).json({error:'Connection not found'});let providerClosed=false;if(c.external_session_id){try{await eb(`/sessions/${encodeURIComponent(c.external_session_id)}`,{method:'DELETE'});providerClosed=true}catch(e){console.warn('ENABLE_BANKING_DISCONNECT',e.message)}}await supa(`bank_accounts?connection_id=eq.${id}`,{method:'PATCH',body:JSON.stringify({active:false})});await supa(`bank_connections?id=eq.${id}`,{method:'PATCH',body:JSON.stringify({status:'disconnected',updated_at:new Date().toISOString()})});res.status(200).json({ok:true,providerClosed})}catch(e){res.status(500).json({error:e.message})}};
